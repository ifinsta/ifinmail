# Rspamd action thresholds — tuned for ifinmail
# Increase defaults slightly to reduce false positives initially
actions {
    greylist = 4.0;
    "add header" = 6.0;
    rewrite_subject = 8.0;
    reject = 15.0;
}
