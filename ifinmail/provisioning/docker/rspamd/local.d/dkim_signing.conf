# Rspamd DKIM signing — one domain block per hosted domain
# Domain → selector → key path mapping
# Add domains here as they are onboarded

allow_username_mismatch = true;
sign_local = true;
use_esld = false;
try_fallback = true;
